{% extends 'base.html.twig' %}

{% block title %}Reset your password{% endblock %}

{% block body %}
    <h1>Herstel het wachtwoord</h1>

    {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        <button class="btn btn-primary">Herstel wachtwoord!</button>
    {{ form_end(resetForm) }}
{% endblock %}