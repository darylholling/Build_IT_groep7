{% extends 'base.html.twig' %}

{% block title %} {% endblock %}

{% block body %}
    <body class="bg-gray-50">
        <div class="container mx-auto ">
            <div class="min-h-screen flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
                <div class="w-full space-y-8 flex items-center justify-center">
                    <div class="w-full lg:w-1/2 bg-white p-5 rounded shadow-md">
                        <h1 class="mb-8 text-3xl text-center font-extrabold">Wachtwoord herstel email verstuurd</h1>
                        <p>Er is een mail verstuurd met een link om het wachtwoord te herstellen. Deze link verloopt in: {{ tokenLifetime|date('g') }} uur.</p>
                        <p>Als u nog niks ontvangen heeft controleer de spam box of <a href="{{ path('app_forgot_password_request') }}">verstuur een nieuwe mail</a>.</p>
                        <div class="mb-6 text-center">
                            <a href="{{ path('app_security_login') }}" class="group relative w-full flex justify-center py-2 px-4 border border-transparent text-sm font-medium rounded-md text-white bg-green-500 hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">Terug naar de hoofdpagina!</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </body>
{% endblock %}
